<|tool_calls_section_begin|> <|tool_call_begin|> functions.Task:0 <|tool_call_argument_begin|> {"summary": "Find and read the debug API and docker compose documentation", "prompt": "Find and read the debug API documentation and docker compose files to understand what is actually supported. Look for:\n1. Debug API endpoints and what they expose\n2. Docker compose topology configuration\n3. Any existing documentation about failover or HA events\n4. The actual docker compose file structure\n5. Any source-backed descriptions of the system snapshot structure"} <|tool_call_end|> <|tool_calls_section_end|>
