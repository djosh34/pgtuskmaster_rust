// todo: replace this entire tool-call artifact with the actual how-to page markdown for `docs/src/how-to/configure-tls.md`.
// todo: stay honest that the shipped cluster configs currently keep both `postgres.tls.mode` and `api.security.tls.mode` disabled, so this must read as an adaptation procedure rather than a shipped enabled-TLS example.
// todo: use only source-supported verification guidance; do not invent certificate-generation scripts or file paths that are not present in the repository.
