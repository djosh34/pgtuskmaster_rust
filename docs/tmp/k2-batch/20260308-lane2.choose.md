Target docs path: `docs/src/reference/runtime-configuration.md`

Diataxis type: `reference`

Why this is the next doc:
- The tutorial uses `runtime.toml` files without explaining their schema
- The reference section lacks documentation for the runtime config format
- Users who complete the first tutorial will need to customize configuration for production
- The code has clear config modules (`src/config/schema.rs`, `src/config/defaults.rs`) that provide authoritative source material
- Runtime config is "machinery" description that fits reference type perfectly
- Existing how-to and CLI reference make this the logical next reference piece

Exact additional information needed:
- file: `src/config/schema.rs`
  why: Contains the Rust structs that define all configuration options and their types
- file: `src/config/defaults.rs`
  why: Shows default values for each configuration field
- file: `src/config/parser.rs`
  why: Reveals validation rules and parsing behavior
- file: `docker/configs/cluster/node-a/runtime.toml`
  why: Working example from the tutorial that demonstrates actual usage
- file: `docker/configs/cluster/node-b/runtime.toml`
  why: Shows node-specific configuration differences
- file: `docker/configs/single/node-a/runtime.toml`
  why: Provides simpler single-node example
- file: `src/cli/args.rs`
  why: Documents command-line arguments that override runtime config
- extra info: What configuration sections are required vs optional? What validation errors are possible?

Optional runtime evidence to generate:
- command: `cargo run --bin pgtuskmaster -- --help`
  why: To see if there are config validation or dump options in the binary
- command: `docker compose --env-file .env.docker.example -f docker/compose/docker-compose.cluster.yml config`
  why: To verify the complete runtime environment variables and mounted config paths used in the tutorial
