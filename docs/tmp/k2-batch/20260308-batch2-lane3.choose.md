Target docs path: `docs/src/reference/runtime-configuration.md`
Diataxis type: reference
Why this is the next doc:
- Configuration controls all subsystems (HA, DCS, API, logging, PostgreSQL) per architecture doc
- No comprehensive configuration reference exists; users cannot discover available options
- Runtime config is central to deployment (shown in docker configs for cluster/single-node)
- Reference material is the most critical gap after basic CLI and architecture docs
- Architecture doc explicitly mentions `src/config/schema.rs` and Docker examples as sources

Exact additional information needed:
- file: `src/config/schema.rs`
  why: Defines complete configuration structure, required/optional fields, and data types
- file: `src/config/defaults.rs`
  why: Shows default values for all configuration parameters
- file: `docker/configs/cluster/node-a/runtime.toml`
  why: Provides complete production cluster configuration example
- file: `docker/configs/single/node-a/runtime.toml`
  why: Shows minimal configuration for single-node deployment
- file: `src/config/parser.rs`
  why: Reveals validation rules and parsing logic that constrain valid values

Optional runtime evidence to generate:
- command: `cargo doc --no-deps --open`
  why: Would generate structured view of config types and fields from schema source code
