Target docs path: `docs/src/reference/runtime-configuration.md`

Diataxis type: reference

Why this is the next doc:
- After tutorial (first cluster) and how-to (health checks), users need to customize configurations
- Runtime configuration is required knowledge for production deployments
- No runtime config reference exists yet in `docs/src/reference/`
- Example configs are used in tutorial but never documented
- Completes the reference section alongside existing CLI reference

Exact additional information needed:
- file: `src/config/schema.rs` - why: defines configuration data structures and fields
- file: `docker/configs/cluster/node-a/runtime.toml` - why: example cluster configuration
- file: `docker/configs/single/node-a/runtime.toml` - why: example single-node configuration
- file: `src/config/defaults.rs` - why: shows default values for optional fields
- file: `src/config/parser.rs` - why: reveals validation rules and parsing logic
- file: `docker/configs/common/pg_hba.conf` - why: shows PostgreSQL authentication context
- extra info: Which configuration fields are required vs optional
- extra info: Secret file reference mechanisms (password file paths)
- extra info: Configuration validation constraints and error messages

Optional runtime evidence to generate:
- command: `cargo run --bin pgtuskmaster -- --help` if it shows config options
  why: would reveal config-related CLI arguments and defaults
- command: `cargo doc --no-deps --open` on config modules 
  why: would generate structured view of config schema types
