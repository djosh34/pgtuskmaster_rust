**Target docs path:** `docs/src/reference/runtime-configuration.md`

**Diataxis type:** reference

**Why this is the next doc:**
- The tutorial and how-to both reference `runtime.toml` files without defining the schema
- There is no authoritative reference for the runtime configuration format, yet it's essential for operating clusters
- The configuration schema is stable in `src/config/schema.rs` and well-structured for reference documentation
- CLI reference already exists; configuration reference is the logical companion
- Test harness and docker configs provide concrete examples that can be used for illustration without inventing content

**Exact additional information needed:**
- file: `src/config/schema.rs` (why: contains the canonical Rust structs defining all configuration fields, types, and optional/required status)
- file: `docker/configs/cluster/node-a/runtime.toml` (why: concrete cluster deployment example showing multi-node settings)
- file: `docker/configs/single/node-a/runtime.toml` (why: concrete single-node example showing minimal config)
- file: `src/config/defaults.rs` (why: defines default values for optional fields)
- file: `src/config/parser.rs` (why: shows validation rules and environment variable handling)

**Optional runtime evidence to generate:**
- command: `cargo doc --document-private-items --package pgtuskmaster_rust --open` (why: would reveal the exact config schema definitions and field documentation via generated Rust docs)
- command: `grep -r "runtime.toml" tests/ --include="*.rs" | head -20` (why: would identify test cases that demonstrate config loading patterns for additional examples)
