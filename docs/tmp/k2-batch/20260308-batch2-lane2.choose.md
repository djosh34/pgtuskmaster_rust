Target docs path: docs/src/reference/runtime-configuration.md
Diataxis type: reference
Why this is the next doc:
- The architecture explanation already references configuration as a core concept and mentions the config schema
- The CLI reference covers one user surface but runtime config is the other primary control surface
- Users cannot operate the system without understanding config sections: cluster, postgres, dcs, ha, process, logging, api, debug
- Example configs exist in docker/configs/ but are not documented as a unified reference
- The config schema in src/config/schema.rs defines the authoritative structure but is not exposed to operators

Exact additional information needed:
- file: src/config/schema.rs
  why: Contains the complete Rust struct definitions for RuntimeConfig and all nested sections
- file: src/config/defaults.rs
  why: Shows default values for each configuration option
- file: docker/configs/cluster/node-a/runtime.toml
  why: Provides a complete real-world example of a multi-node cluster configuration
- file: docker/configs/single/node-a/runtime.toml
  why: Shows minimal single-node configuration for contrast
- extra info: Which config options are required vs optional
- extra info: Validation rules or constraints for each option

Optional runtime evidence to generate:
- command: cargo run --bin pgtuskmaster -- --help
  why: Would show command-line flags that override config file settings
- command: cargo doc --no-deps --open
  why: Would generate rustdoc for config module to cross-check schema definitions
