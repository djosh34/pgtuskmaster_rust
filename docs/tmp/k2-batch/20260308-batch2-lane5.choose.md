Target docs path: `docs/src/reference/runtime-configuration.md`
Diataxis type: reference

Why this is the next doc:
- Configuration is fundamental to operating pgtuskmaster; users must understand config options to deploy, tune, and troubleshoot
- No comprehensive config reference exists yet; architecture.md only mentions config sections briefly
- Example configs exist in docker/configs/ but are undocumented
- A config reference is a prerequisite for most operational tasks beyond basic cluster startup
- It fits reference type: information-oriented, describing the machinery, meant for consultation during work

Exact additional information needed:
- `src/config/schema.rs`: contains the definitive config schema with all sections, fields, types, and validation rules
- `src/config/defaults.rs`: provides default values for each configuration field
- `docker/configs/cluster/node-a/runtime.toml`: shows a complete multi-node cluster configuration example
- `docker/configs/single/node-a/runtime.toml`: shows a minimal single-node configuration example
- `src/lib.rs`: to understand the module organization and public API boundaries that the config affects

Optional runtime evidence to generate:
- command: `cargo doc --no-deps --open` (or grep equivalent) to extract struct/field definitions from schema.rs
  why: would provide precise type information and doc comments from the source
- command: `find docker/configs -name "*.toml" -exec echo "=== {} ===" \; -exec cat {} \;`
  why: would show all example config variants in the repository for comparison
