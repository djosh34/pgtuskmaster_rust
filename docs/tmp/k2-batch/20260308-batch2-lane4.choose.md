Target docs path: docs/src/reference/runtime-configuration.md
Diataxis type: reference
Why this is the next doc:
- Runtime configuration is fundamental to operating pgtuskmaster; every deployment requires a valid runtime.toml
- No dedicated reference exists; architecture.md only mentions config sections without schema details
- Completes the Reference section alongside pgtuskmasterctl-cli.md
- Schema definitions in src/config/schema.rs provide complete, authoritative source for all fields, types, and structure
- Example configs in docker/configs/ provide real-world usage patterns
- Default values are defined in src/config/defaults.rs, which is essential reference material

Exact additional information needed:
- file: src/config/schema.rs
  why: Contains the canonical RuntimeConfig struct and all subsection schemas (cluster, postgres, dcs, ha, process, logging, api, debug) with field names, types, and inline documentation
- file: src/config/defaults.rs
  why: Defines default values for optional fields; required to document what can be omitted and what defaults apply
- file: docker/configs/cluster/node-a/runtime.toml
  why: Provides a complete, production-ready example showing all populated config sections and realistic values
- file: docker/configs/single/node-a/runtime.toml
  why: Shows minimal required config for a single-node setup, useful for distinguishing mandatory vs optional settings
- file: src/config/parser.rs
  why: Reveals validation rules, TOML parsing constraints, and error conditions for invalid configurations

Extra info:
- What is the purpose and versioning semantics of config_version = "v2"?
- Are any config fields overrideable by environment variables?
- What validation errors can occur during config loading?

Optional runtime evidence to generate:
- command: cargo run --quiet --bin pgtuskmaster -- --help
  why: To determine if a config validation or lint mode exists that can be documented
