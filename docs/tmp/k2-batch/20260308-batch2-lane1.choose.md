Target docs path: docs/src/reference/runtime-configuration.md
Diataxis type: reference
Why this is the next doc:
- The architecture doc explicitly identifies runtime configuration as shaping "almost every subsystem"
- Users completing the first cluster tutorial need authoritative runtime.toml reference to customize deployments
- No existing reference documents the configuration schema, despite multiple example configs in docker/configs/
- Provides essential complement to the existing pgtuskmasterctl CLI reference

Exact additional information needed:
- file: docker/configs/cluster/node-a/runtime.toml
  why: Primary example of multi-node cluster configuration
- file: docker/configs/single/node-a/runtime.toml
  why: Example of single-node configuration for contrast
- file: src/config/schema.rs
  why: Authoritative source for all configuration sections and fields
- file: src/config/defaults.rs
  why: Documents default values where applicable
- file: src/config/parser.rs
  why: Shows validation rules and parsing behavior
- extra info: Which config_version values are supported and their behavioral differences?

Optional runtime evidence:
- command: grep -rn "config_version" docker/configs/
  why: Verify version declaration patterns across all example configs
- command: rg "pub struct Config" src/config/schema.rs -A 20
  why: Extract top-level configuration structure and nested sections
