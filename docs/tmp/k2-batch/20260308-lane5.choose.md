<|tool_calls_section_begin|> <|tool_call_begin|> <|tool_calls_section_begin|> <|tool_call_begin|> <|tool_calls_section_begin|> <|tool_call_begin|> Target docs path: docs/src/reference/runtime-configuration.md
Diataxis type: reference
Why this is the next doc:
- We have tutorial, how-to, and one reference (CLI), but no configuration reference
- Runtime configuration is core machinery that operators must understand to deploy
- Reference documentation should describe the machinery systematically
- Users need this to customize deployments beyond the tutorial's defaults
- This completes the basic operational documentation quadrants

Exact additional information needed:
- file: src/config/schema.rs
  why: Contains the complete RuntimeConfig, EtcdConfig, LoggingConfig, ApiConfig, TlsServerConfig, PostgresConfig structs with all fields, types, and nested structures
- file: src/config/defaults.rs
  why: Defines all default values for each configuration field, essential for reference documentation
- file: docker/configs/cluster/node-a/runtime.toml
  why: Real cluster deployment example showing multi-node configuration patterns
- file: docker/configs/single/node-a/runtime.toml
  why: Real single-node deployment example showing minimal configuration

Optional runtime evidence to generate:
- command: cargo run --bin pgtuskmaster -- --help
  why: Verify if binary accepts runtime config path as CLI argument or uses fixed location (currently appears to use fixed `<data_dir>/configs/runtime.toml` per source inspection)
