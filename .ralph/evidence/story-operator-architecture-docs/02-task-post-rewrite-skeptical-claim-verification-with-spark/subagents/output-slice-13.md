# output-slice-13

| claim_id | status | evidence_anchor | notes |
|---|---|---|---|
| claim-0008 | verified | `src/ha/decide.rs:20-31` (`decide` reads `world.dcs.value.trust`, `world.dcs.value.cache.leader`, `world.dcs.value.cache.switchover`), `src/ha/decide.rs:45-58` (adds DCS trust gating), `src/dcs/state.rs:87-103` (`evaluate_trust`) | Verified as shorthand: the decision function explicitly consumes DCS-derived trust and cache signals when selecting HA behavior. |
| claim-0009 | verified | `src/ha/worker.rs:72-80` (step path always builds `WorldSnapshot` and calls `decide`), `src/ha/worker.rs:83-84` (publishes `output.next`), `src/ha/decide.rs:20-31` (`decide` input includes DCS trust/cache), `src/ha/decide.rs:51-59` | Verified: DCS trust + cache are direct, explicit inputs to the decision engine (`decide`) through the standard HA worker step path. |
| claim-0010 | verified | `src/ha/worker.rs:99-129` (`dispatch_actions` maps `AcquireLeaderLease`, `ReleaseLeaderLease`, `ClearSwitchover` to DCS store operations), `src/ha/worker.rs:1134-1157` (`dispatch_actions_maps_dcs_and_process_requests` asserts one leader write and one delete from actions) | Verified: transitions produced by `decide` become concrete action outputs, and decision outputs directly trigger DCS write/delete operations in worker dispatch. |
| claim-0011 | uncertain-with-followup | `src/ha/worker.rs:72-80` (all periodic HA steps go through `decide`), `src/ha/decide.rs:20-217` (decision matrix and transitions), `src/api/controller.rs:13-49` (`post_switchover` writes `/scope/switchover`), `src/api/worker.rs:191-214` (control/observation API routes) | Uncertain: internal HA phase progression is centrally decided, but external explicit API mutations into DCS (`/switchover`, `/ha/switchover`) are also valid control inputs; that means not all transition impetus is single internal source, only single internal decision model for worker-driven transitions. Follow-up: add explicit trace assertions proving which state transitions can only originate from `decide` outputs. |
| claim-0022 | rewritten | `src/ha/decide.rs:51-59` (ambiguous/unsafe coordination drives FailSafe), `src/config/parser.rs:575-606` (rejects weak/invalid DCS and HA parameters), `src/ha/decide.rs:390-445` (failure/tension paths explicitly fence or fail-safe) | Rewritten wording: “The implementation is conservative under ambiguous or underspecified coordination by forcing `FailSafe` in non-full quorum situations and by hard-failing malformed core configuration before runtime.” |
| claim-0023 | verified | `src/dcs/state.rs:87-103` (`evaluate_trust` requires healthy store + membership consistency), `src/ha/decide.rs:51-59` (trust gates major HA transitions), `src/dcs/keys.rs:26-33,85-91` (scope-prefix check and wrong-scope rejection test coverage) | Verified: coordination behavior is bounded by etcd/DCS health and scope-correct key parsing, and tests explicitly cover wrong-scope rejection. |
| claim-0024 | uncertain-with-followup | `src/ha/decide.rs:148-190` (rewind/bootstrap/fencing transition branching), `src/ha/decide.rs:420-431`, `src/runtime/node.rs:275-288` (clone/init startup decisions based on healthy leader availability), `src/process/worker.rs:614-667` (basebackup/rewind command specs require source host/user/dbname and network-oriented connection args), `src/process/worker.rs:560-563` (timeout selection for rewind/bootstrap jobs) | Uncertain: code clearly shows recovery path choice depends on rewind/bootstrap job outcomes and command prerequisites, but the claim’s “recovery speed depends on” is not directly measured/guarded by an explicit metric in repo code. Follow-up: add a test asserting timeout/path-shape behavior under source connectivity failure to close this gap. |
| claim-0134 | uncertain-with-followup | `src/runtime/node.rs:657-753` (runtime starts workers and API), `src/api/worker.rs:190-214` (control-observation routes, including `/switchover` and `/ha/state`), `src/cli/client.rs:74-102` (CLI calls `/ha/state`, `/switchover`, `/ha/switchover`) | Uncertain: repository evidence confirms control/observation interfaces exist, but no explicit test/runtime check defines or validates the “three-step” quick-start flow as a mandatory sequence. Follow-up: add an integration/e2e test that executes an explicit three-step onboarding scenario with explicit pass/fail assertions. |

