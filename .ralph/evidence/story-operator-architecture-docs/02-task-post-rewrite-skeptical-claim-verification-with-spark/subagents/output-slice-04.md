# output-slice-04

| claim_id | status | evidence_anchor | notes |
|---|---|---|---|
| claim-0012 | rewritten | `src/dcs/keys.rs` (DcsKey + key_from_path) | Rewritten wording: “The DCS record surface is defined in code (`DcsKey`, `key_from_path`, and path builders in `src/dcs/store.rs`), not by documentation headers.” |
| claim-0013 | verified | `src/dcs/keys.rs` (`DcsKey` variants member/leader/switchover/init-lock/config); `src/dcs/store.rs` (path constructors and typed write methods) | The code enumerates concrete DCS record classes and has typed write paths that assert what gets written; this is also validated by unit tests in `store` (e.g., member-path-only assertions and per-writer path correctness). |
| claim-0014 | verified | `src/dcs/keys.rs` scoped key parsing/path forms | `key_from_path` enforces `/<scope>/...` style keys and rejects malformed paths, and unit tests confirm accepted/rejected path formats, including scoped member/leader/switchover keys. |
| claim-0015 | verified | `src/dcs/store.rs` `write_local_member`; `src/dcs/worker.rs` membership cache/write on local state change; `src/dcs/store.rs` `write_local_member_writes_only_member_path` test | The DCS worker path writes member state via `write_local_member`, and store tests assert it only targets the member path (owner and namespace are explicit in writer path builders). |
| claim-0016 | verified | `src/dcs/store.rs` `write_leader_lease`/`delete_leader`; `src/ha/worker.rs` `dispatch_actions` leader-lease actions | Leader coordination writes are done through `DcsHaWriter` methods (`write_leader_lease`, `delete_leader`) invoked from HA worker action dispatch. |
| claim-0017 | verified | `src/dcs/worker.rs` `step_once` observes local PG state and updates local member state; `src/dcs/worker.rs` guards around unnecessary writes | The DCS worker does write membership updates tied to local PostgreSQL state transitions (not on every tick), which is consistent with the claim’s intent and confirmed by worker tests around self-member writes/version change conditions. |
| claim-0018 | verified | `src/ha/decide.rs` lifecycle decision output; `src/ha/worker.rs` executes lease/switchover clear actions | HA decision logic emits coordination actions based on lifecycle/fencing states and the HA worker executes those against DCS writer actions, so leader coordination is updated by HA lifecycle decisions. |
| claim-0019 | rewritten | `src/dcs/store.rs` path discipline tests for member/leader/switchover writes/deletes | Rewritten wording: “Writer APIs are path-scoped and tests enforce path discipline (`member`, `leader`, `switchover`), which constrains hidden side effects between components.” |
| claim-0020 | verified | `src/dcs/state.rs` `evaluate_trust`; `src/ha/decide.rs` behavior gating under degraded trust | Trust status is a first-class input to control flow and role behavior is intentionally conservative when trust is degraded (`NotTrusted`/`FailSafe`), matching the claim. |
| claim-0021 | verified | `src/api/controller.rs` switchover submission writes; `src/api/worker.rs` `DELETE /ha/switchover` clears; `src/ha/worker.rs` switchover-clear handling; `src/ha/decide.rs` stale/terminal switchover corrective path | The ownership split is concrete in code (`/switchover` writes are API-driven, `/leader` writes are HA-driven). |

